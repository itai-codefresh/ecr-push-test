
version: '1.0'
#re-adding comments
steps:
  
  build_step:
    type: build
    description: It builds the Djanga Service
    dockerfile: Dockerfile
    image_name: itaicodefresh/bamigrash
    no_cache: ${{NO_CACHE}}
  
  push_to_registry:
    type: push
    candidate: ${{build_step}}
    tag: ${{CF_BRANCH}}
    image_name: ma/ma/yokero/bamigrash
    registry: myecr
      
  #push_to_registry:
    #type: push
    #provider: ecr
    #accessKeyId: ${{accessKeyId}}
    #secretAccessKey: ${{secretAccessKey}}
    #region: us-east-1
    #candidate: ${{build_step}}
    #tag: ${{CF_BRANCH}}
